import { makeStyles } from '@material-ui/core';
import './styles.css';

const calendarFooterStyles = makeStyles({
  root: {
    display: 'flex',
    justifyContent: 'center',
    margin: '16px 0px'
  }
});

export default calendarFooterStyles;
